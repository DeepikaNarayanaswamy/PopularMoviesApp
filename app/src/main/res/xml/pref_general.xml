<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="@string/pref_value_Popular"
        android:entries="@array/pref_sort_by_titles"
        android:entryValues="@array/pref_sort_by_list_values"
        android:key="@string/pref_sort_by_key"
        android:title="@string/pref_sort_by" />

</PreferenceScreen>
